export const route = () => {
    const routes = {
        '/': 'home.js',
        '/profile': 'profile.js',
        '/search': 'search.js',
    }

    console.log(location);
    const path = location.pathname;

}